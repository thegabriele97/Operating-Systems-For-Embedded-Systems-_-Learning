#include "ee.h"
#include "Arduino.h"
#include "HelpfulSerial.h"

HelpfulSerial *serial;

void loop(void) {
	/*ActivateTask(TaskL1);*/
}

void idle_hook(void) {
	loop();
	if (serialEventRun)
		serialEventRun();
}

void setup(void) {
	//Serial.begin(9600);

	/*Serial.println("ciao");
	if (serialEventRun)
			serialEventRun();*/
}

int main(void) {

	init();

	serial = new HelpfulSerial(115200);
	//serial->begin();
	Serial.begin(115200);
	serial->println("code.cpp: Hello from the main function!!");


	setup();

#if defined(USBCON)
	USBDevice.attach();
#endif

	StartOS(OSDEFAULTAPPMODE);

	return 0;

}

