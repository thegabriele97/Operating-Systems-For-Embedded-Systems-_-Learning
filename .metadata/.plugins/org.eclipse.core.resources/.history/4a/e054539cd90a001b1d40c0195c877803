#include "ee.h"
#include "Arduino.h"
#include "MyArduino.h"
#include "stdio.h"

void StartupHook() {
	char buff[BUFFSIZ];

	DDRD |= (MASK(3) | MASK(7));
	PORTD &= ~(MASK(3) | MASK(7));


	sprintf(buff, "hook.cpp: DDRD=[0x%x]\tPORTD=[0x%x]", DDRD, PORTD);
	Serial.println(buff);
}
